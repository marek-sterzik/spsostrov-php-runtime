stages:
  - build

build:
  stage: build
  cache:
    key:
      files:
        - composer.lock
    paths:
      - vendor
  rules:
    - if: $CI_COMMIT_TAG
      when: never
    - when: always
  script:
    - build-script
