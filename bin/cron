#!/bin/bash

period=60

invoke() {
    lib/console job:run --include-cron &
}

echo "cron running"

invoke

while true; do
    time="$(expr "$period" - \( "$(date +%s)" % "$period" \) )"
    sleep "$time"
    invoke
done
