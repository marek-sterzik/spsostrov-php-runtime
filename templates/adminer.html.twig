{% extends "base.html.twig" %}
{% block wide_body %}
    <div class="adminer-link"><a href="{{ path("app.adminer") }}" target="_blank">otevřít adminer v novém okně</a></div>
    <div class="adminer-frame">
        <iframe src="" data-src="{{ path("app.adminer") }}" title="adminer"></iframe>
    </div>
{% endblock %}
